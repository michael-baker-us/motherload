/**
 * Shared UI font stacks (see src/assets/fonts.css for the @font-face defs).
 * `FONT_UI` is the monospace body/HUD face — it keeps numeric readouts aligned;
 * `FONT_DISPLAY` is the wide face for big titles and headers. Both fall back to
 * the platform monospace until the web fonts load.
 */
export const FONT_UI = '"Share Tech Mono", ui-monospace, SFMono-Regular, Menlo, monospace';
export const FONT_DISPLAY = '"Orbitron", "Share Tech Mono", ui-monospace, monospace';
